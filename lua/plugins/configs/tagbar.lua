return {
  {
    'preservim/tagbar',
    cmd = 'TagbarToggle', -- Lazy load Tagbar when the command is run
    keys = { { '<leader>tb', ':TagbarToggle<CR>', desc = 'Toggle Tagbar' } } -- Optional keymap
  }
}

